{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the MarsTV main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "stronghold:default"
  ]
}
